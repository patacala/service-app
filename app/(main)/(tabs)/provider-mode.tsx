import { ScreenWrapper } from '@/design-system/layout/ScreenWrapper';
import { ProvModeScreen } from '@/features/provMode';

export default function ProviderModePage() {
  return (
    <ScreenWrapper>
      <ProvModeScreen />
    </ScreenWrapper>
  );
}