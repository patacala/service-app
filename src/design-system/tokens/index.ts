import {tokens} from './generated/tokens';

// Exportamos los tokens tipados para el tema
export const {colors, spacing, border, typography} = tokens;

// Re-exportamos los tokens completos
export type {Tokens} from './generated/tokens';
