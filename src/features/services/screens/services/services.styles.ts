import { StyleSheet } from 'react-native';

export const getServicesStyles = StyleSheet.create({
    scrollView: {
        flex: 1,
        marginTop: 16,
    },
    scrollContent: {
        paddingBottom: 30
    }
});